#! /usr/bin/env gforth
[undefined] v.VF [IF]
   S" gforth" ENVIRONMENT? [IF]
      warnings off 2drop include ../../vf/gforth.fs warnings on
[THEN] [THEN]
.( FdTimes )
INCLUDE FdTimes.vf
\\

[STARTUP]
INCLUDE=FdTimes.vf
TITLE=FdTimes
